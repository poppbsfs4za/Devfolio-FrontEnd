services:
  frontend:
    image: devfolio-frontend-local
    ports:
      - "3001:8080"
    environment:
      BACKEND_ORIGIN: http://host.docker.internal:8080
      NEXT_PUBLIC_API_BASE_URL: ""
      INTERNAL_API_BASE_URL: http://localhost:3001